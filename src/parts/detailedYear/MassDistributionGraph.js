import React from 'react'
import { applyProps } from '../../../node_modules/react-three-fiber';

export const MassDistributionGraph = (props) => {

    return (
        <section>
            <h2 className="titleDetailedYear">Year {props.year}'s meteors </h2>
        </section>
    )
}